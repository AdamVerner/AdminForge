namespace AdminForge.Middleware;

/// <summary>
/// Phase 0 placeholder. Real middleware (auth policy enforcement, route mounting) lands in Phase 2.
/// </summary>
internal sealed class AssemblyMarker;
